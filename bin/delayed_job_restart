#!/usr/bin/env ruby

require 'rubygems'

cli_arg = ARGV.shift

inc = ''

if cli_arg == '-I'
  inc = ARGV.shift
  $LOAD_PATH.concat inc.split(":")
end

module Delayed; end

Delayed.const_set('RESTART_ARGS', ['-I', inc])

require 'delayed/command'
Delayed::Command.new(ARGV).run
